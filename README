WRF Web App
============
name: Lucy Mahlmann
eid: lam6744
bitbucketusername: Lmahlmann
app: https://ec2-3-141-1-150.us-east-2.compute.amazonaws.com:5009/
comments: - I added line 285 in order to stop the ETL from erroring

public DNS of instance: ec2-3-141-1-150.us-east-2.compute.amazonaws.com

TODO list
- when deploy aws ec2 add admin and add cities, austin and dallas
- was i supposed to copy over the austin.json, dallas.json, and houston.json files from assignment4?
- implement having multiple users with different passwords
- set up the aws vm 
- make sure that it is correctly loading the data for the cities
- revert the \data back to /data before copying to the aws vm
- update app url before turning in and update uri in google cloud oauth
-  delete data and -p folders before copying to aws vm
- delete user3.json
-  delete this part vv
6. scp -i “weatherportal-key.pem” assignment5.tar.gz ubuntu@ec2-3-141-1-150.us-east-2.compute.amazonaws.com:~/.
7. ssh -i “weatherportal-key.pem” ubuntu@ec2-3-141-1-150.us-east-2.compute.amazonaws.com
• On AWS EC2 VM:
1. gunzip assignment5.tar.gz
2. tar –xvf assignment5.tar
3. cd assignment5
4. source venv/bin/activate
5. pip3 install -r requirements.txt
6. nohup python3 application.py &
7. exit


Run 
----
1. python3 -m venv venv
2. source venv/bin/activate
3. For Google OAuth:
   - pip3 install --only-binary :all: greenlet
   - pip3 install --only-binary :all: pyopenssl
3. pip3 install -r requirements.txt
4. python3 application.py


Try
---
1. Open application url: 
   - Non ssl: http://localhost:5009/
   - Ssl: https://localhost:5009/
2. Use REST API to:
   - register admin
   - register users
   - register cities
3. Access the web app in the browser to test OAuth integration
4. Logout
5. Login again using same user.

Stop
----
1. Ctrl+C in the window where application.py was started


